import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.iMeal',
  appName: 'iMealV2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
