import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.iMealV2',
  appName: 'iMealV2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
